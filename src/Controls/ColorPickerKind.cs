namespace Zhai.Famil.Controls
{
    public enum ColorPickerKind
    {
        Normal,
        RGB,
        HSV,
        HSL,
        HSV2,
        HSL2,
    }

    public enum RGB
    {
        R, G, B, A,
    }

    public enum HSV
    {
        H, S, V,
    }

    public enum HSL
    {
        H, S, L,
    }
}
